jQuery(document).ready(function( $ ) {
	'use strict';

	/*var onloadCaptchaCallback = function() {
		grecaptcha.render('g-recaptcha', {
			'sitekey' : BPRGC_Public_JS_Obj.site_key,
			'theme' : BPRGC_Public_JS_Obj.site_key,
			'type' : 'image'
		});
	};*/
});
